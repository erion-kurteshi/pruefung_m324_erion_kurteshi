{
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write .",
    "format-check": "prettier --check .",
    "start": "npm run lint && npm run format-check && node index.js && npm run convert",
    "convert": "node convert.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "eslint": "^9.16.0",
    "marked": "^15.0.3",
    "prettier": "^3.4.2"
  }
}
